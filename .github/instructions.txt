Stage F is approved for commit pending one final lightweight verification.

Do NOT audit again.
Do NOT modify any files.
Do NOT commit.
Do NOT push.

Run only:

1. `git status`
2. `git diff --check`
3. `node --test`
4. `git diff --stat`

Confirm:

* 135 tests pass
* 0 failures
* diff-check exit 0

Also confirm `.env` is NOT among the files being committed.

Then stop.
